machine:
  pre:
    - wget https://s3.amazonaws.com/travis-phantomjs/phantomjs-2.0.0-ubuntu-12.04.tar.bz2
    - tar -xjf phantomjs-2.0.0-ubuntu-12.04.tar.bz2
dependencies:
  override:
    - npm install -D gulp gulp-shell run-sequence n1k0/casperjs.git#master
test:
  override:
    - npm run e2e-test